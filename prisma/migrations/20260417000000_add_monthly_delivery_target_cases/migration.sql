-- Placeholder for a migration that was applied directly to the production
-- database but never committed to this repo. Recovered as an empty migration
-- so that `prisma migrate status` no longer reports it as missing from the
-- local history. Do NOT add SQL here — prod has already recorded this name
-- as applied.
SELECT 1;
